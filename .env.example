VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
PORT=3000

# Optional: if set, GET /api/cron/tick requires this value in the
# x-cron-secret header. Leave unset for local dev.
CRON_SECRET=
